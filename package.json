{
  "name": "s3-upload-processing-cdk-pipeline",
  "version": "1.0.0",
  "description": "Demonstrates how to upload directly to an S3 bucket and use a Lambda to process the uploaded content.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "workspaces": [
    "app",
    "deploy",
    "lambda-fns"
  ]
}
